services:
  claude-ws:
    build: .
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - CLAUDE_PROVIDER=sdk
      - PORT=8556
    expose:
      - "8556"
    volumes:
      - claude-ws-data:/app/data

volumes:
  claude-ws-data:
